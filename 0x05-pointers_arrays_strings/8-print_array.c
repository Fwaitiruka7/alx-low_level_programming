#include "main.h"
#include <stdio.h>

/**
 * print_array - prints n elements of an array of integers
 * @a: array to be used
 * @n: number of elements of the array to be printed
 */

void print_array(int *a, int n)
{
	int ria;
	for (ria = 0; ria < ( n - 1); ria++)
		printf("%d, ", a[i]);
	printf("\n");
}
