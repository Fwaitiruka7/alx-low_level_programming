#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: 'the program's description'
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int number_x;

	/* for loop statement for  single digit decrement */

	for (number_x = 0; number_x < 10; number_x++)
		printf("%d", number_x);

	printf("\n");

	return (0);
}
